# packages 目录说明

为每位访问者创建一个与密码完全一致的文件夹，并将准备好的压缩包命名为 `content.zip` 放在该文件夹内。例如：

```
packages/
  user001/content.zip
  vip02/content.zip
```

推送到 GitHub 后，访问者在网页输入 `user001` 即会下载 `packages/user001/content.zip`。
